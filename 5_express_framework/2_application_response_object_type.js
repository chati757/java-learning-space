//response part

var express = require('express');
var app = express();

// app.set(name,value); //set env variable work on runtime

// app.get(name); //get env variable

// app.use(path,callback); //handle request(all request) that path and callback middleware

// app.get(path,callback); //handle get request and callback middleware

// app.post(path,callback); //handle post request and callback middleware

// app.put(path,callback); //handle put request and callback middleware

// app.delete(path,callback); //handle delete request and callback middleware

/*
app.route(path); //chain function
    .get(callback)
    .post(callback);
*/